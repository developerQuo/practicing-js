import { describe, expect, test } from '@jest/globals';

describe('주어진 숫자까지의 모든 숫자 더하기', () => {
	describe('it sums numbers', () => {
		test('for loop', () => {});
		test('recursion', () => {});
		test('등차수열 공식', () => {});
	});
});
