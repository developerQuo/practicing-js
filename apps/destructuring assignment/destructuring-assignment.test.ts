import { describe, expect, it } from '@jest/globals';
import destructuringAssignment from './destructuring-assignment';

describe('구조 분해 할당', () => {
	it('converts user info to customer info', () => {
		let user = {
			name: 'John',
			years: 30,
		};
	});
});
